/**
 * @file Barrel exports for the JobMetadataFooter component.
 *
 * Re-exports all public types, components, and utilities from the
 * JobMetadataFooter module so consumers can import from the parent
 * directory instead of reaching into the module internals.
 */

export * from './JobMetadataFooter';
