/**
 * @file index.ts
 * @description Barrel module that re-exports the {@link JobRow} component and all
 *   associated public types from `./JobRow`. Consumers should import from this
 *   barrel rather than from the implementation file directly.
 */

export * from './JobRow';
