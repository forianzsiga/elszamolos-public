/**
 * @file Barrel module for the JobManagerHeader component.
 * Re-exports the JobManagerHeader component from the JobManagerHeader module.
 */

/**
 * JobManagerHeader — A component that renders the header area of the Job Manager panel.
 *
 * Displays the page title, a JobStatusSummary, and action buttons for:
 * - Adding a manual job entry
 * - Importing/exporting JSON (developer mode only)
 * - Importing a folder of files
 * - Recalculating job totals
 */
export * from './JobManagerHeader';
