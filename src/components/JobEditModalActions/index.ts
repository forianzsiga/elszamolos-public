/**
 * @file Barrel module for JobEditModalActions.
 *
 * Re-exports the `JobEditModalActions` component, which provides the
 * action buttons (Cancel, Save/Create, Delete) rendered inside a job
 * editing modal's dialog footer.
 *
 * @packageDocumentation
 */

/**
 * Public API surface.
 *
 * - `JobEditModalActions` – the named component export.
 * - All other named exports from the source file (none currently).
 */
export * from './JobEditModalActions';

/**
 * Default export – the `JobEditModalActions` component.
 */
export { default } from './JobEditModalActions';
