/**
 * @file InvoicePreview barrel module.
 *
 * Re-exports all public API from the InvoicePreview component module.
 * Consumers should import from this barrel rather than from the
 * implementation file directly.
 *
 * @example
 * // Preferred usage:
 * import { InvoicePreview } from 'components/InvoicePreview';
 *
 * @see {@link ./InvoicePreview} for the full component implementation.
 */

export * from './InvoicePreview';
