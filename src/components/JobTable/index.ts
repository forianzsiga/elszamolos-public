/**
 * @file index.ts
 * Barrel module for the JobTable component.
 * Re-exports the {@link JobTable} component from the adjacent JobTable module
 * so that consumers can import it via the directory path.
 */

/** Re-exports all public members from the {@link ./JobTable} module. */
export * from './JobTable';
