/**
 * @file GlobalToast barrel module.
 * Re-exports the {@link GlobalToast} component from './GlobalToast'.
 * The GlobalToast component renders a stack of auto-dismissing toast
 * notifications (powered by LogContext) with severity-based styling,
 * slide/collapse transitions, a progress bar, and optional action
 * buttons for editing rules or viewing logs.
 */

export { GlobalToast } from './GlobalToast';
