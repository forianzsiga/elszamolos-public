/**
 * @file Barrel module for the CustomScrollbarStyles component.
 * Re-exports the {@link CustomScrollbarStyles} component which applies
 * custom, theme-aware scrollbar styling across the entire application.
 */

/**
 * Custom global scrollbar styles component.
 * Renders MUI {@link https://mui.com/system/styles/ GlobalStyles} with
 * CSS rules targeting both WebKit and Firefox scrollbar pseudo-elements,
 * adapting to the current light/dark theme palette.
 */
export * from './CustomScrollbarStyles';
