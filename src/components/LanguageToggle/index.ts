/**
 * @file LanguageToggle barrel module.
 * Aggregates and re-exports the public API of the LanguageToggle component.
 */

/**
 * LanguageToggle component.
 *
 * Renders a language switcher button with a dropdown menu. Displays the
 * current language code next to an icon and lists available languages
 * (English, Magyar, and optionally Debug in developer mode). The selection
 * updates the global language context.
 */
export * from './LanguageToggle';
