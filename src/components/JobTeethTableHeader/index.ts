/**
 * @file Barrel export for the JobTeethTableHeader component.
 * Re-exports the default export from the JobTeethTableHeader module
 * as a named export for convenient importing.
 */

/**
 * A table header component for the job teeth table.
 *
 * Displays the table title along with action buttons (expand and column
 * visibility selector) wrapped in responsive tooltips.
 */
export { default as JobTeethTableHeader } from './JobTeethTableHeader';
