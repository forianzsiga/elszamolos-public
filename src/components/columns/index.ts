/**
 * @file Barrel module for teeth table column definitions.
 *
 * Re-exports all column-related utilities and hooks from the columns module,
 * including the `useTeethColumnDefs` hook used to build the column configuration
 * for the job teeth virtual data table.
 */

export * from './columns';
