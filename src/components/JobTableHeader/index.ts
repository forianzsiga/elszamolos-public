/**
 * @file Barrel module for the JobTableHeader component.
 * Re-exports the {@link JobTableHeader} component and its associated types
 * (such as {@link JobTableHeaderProps}) from the sibling `./JobTableHeader` module.
 */

/**
 * Re-export all named exports from the JobTableHeader module.
 *
 * This includes the {@link JobTableHeader} component and the
 * {@link JobTableHeaderProps} interface.
 */
export * from './JobTableHeader';

/**
 * Re-export the default export from the JobTableHeader module.
 *
 * @returns The default-exported {@link JobTableHeader} component.
 */
export { default } from './JobTableHeader';
